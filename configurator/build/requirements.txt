# Exact environment used to generate the committed configurator assets.
# OCP is pinned because a different OCCT tessellator can change plate.glb.
build123d==0.11.1
cadquery-ocp-novtk==7.9.3.1.1
numpy==2.5.1
Pillow==12.2.0
